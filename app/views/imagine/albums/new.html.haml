= form_for @album do |f|
  %fieldset
    %legend Create a new album
    .clearfix
      = f.label :name
      .input
        = f.text_field :name
    .actions
      = f.submit :class => 'btn primary'
